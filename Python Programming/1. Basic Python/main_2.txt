
# Belajar Python [Dasar] - 54 - Import Statement
# fungsinya :adalah untuk mengambil program dari file eksternal.py

# 1. untuk menyambung program daru external
#   ---> ada program print di file line,kita bisa run 
#   ---> disini dengan formart : import (nama file)
#   ---> cth : import main_py1
# 2. import dengan data
#   ---> jika ingin import dengan data/variable gunakan
#   ---> format : import (nama file)
#   --->          (namafile).variable
# 3. import dengan fungsi
#   ---> jika ada fungsi di file lain kita bisa masukan/run
#   ---> disini
#   ---> dengan format : import (nama file)
#   ---> lalu kita run bisa dengan format :
#   ---> variable = (namafile).(nama fungsi) lalu print

# Belajar Python [Dasar] - 55 - Membuat Module

# module matematika dengan import
# import matematika 
# 
# hasil_tambah = matematika.tambah(1,2,3,4,5)
# print(f"hasil tambah = {hasil_tambah}")
# 
# hasil_kali = matematika.kali(1,2,3,4,5)
# print(f"hasil kali = {hasil_kali}")
# 
# pangkat_3 = matematika.pangkat(3)
# print(f"hasil pangkat3 = {pangkat_3(3)}")

# kita bisa mengakses module lebih gampang dengan 
# from (nama file) import (fungsi,fungsi,...)
# atau kita bisa mengimport semuanya dengan 
# from (nama file) import * dan jika seperti itu
# file (matematika) kita tidak perlu menuliskannya lagi

# kita juga bisa menggunakan alias 
# cth : from matematika import tambah as y
# ketika kita ganti hasil_tambah = y(....)
# kita bisa aliasan juga nama filenya
# itu akan berjalan normal

# Belajar Python [Dasar] - 56 - Membuat Package Sederhana

# package : tempat tempat menaruh module module 
# cara membuat package dengan membuat folder baru
#   ---> import (nama folder).(nama file)//from (nama folder) import (nama file)
#   ---> ikuti cara di pelajaran 55 untuk penggunaan 
# kenapa ada (_pychache_)? itu adalah untuk menaruh bytecode
# ketika kita memanggil module lagi itu akan langsung dari
# pychache dan lebih cepat

# Belajar Python [Dasar] - 57 - __Init__.py pada Package

# _init_.py: adalah file yang harus ada,kadang ada kadang juga nggak
# _init_.py berfungsi untuk menempelkan folder ke file 
# kita tidak bisa membuat program seperti import(nama folder) tanpa nama file
# tapi dengan _init_ kita bisa,format: from . import(nama file)
# begitu kita jalankan import folder tanpa nama file itu tidak akan error

# Belajar Python [Dasar] - 58 - Menggunakan Standard Library
# import datetime

# data_waktu = datetime.datetime.now()
# import datetime

# # Mendapatkan waktu saat ini
# data_waktu = datetime.datetime.now()

# # Menampilkan format waktu yang benar
# print(f"Tanggal sekarang: {data_waktu.strftime('%A')} - {data_waktu.day} - {data_waktu.strftime('%B')} - {data_waktu.year}")

# from collections import Counter

# data = ["a","p","d","e","q","u","w","s","a"]
# data_count = Counter(data)

# print(f"data count:{data_count}")
# print(f"jumlah a = {data_count['a']}")

# import io
# file = io.open("file-text.txt","r")
# print(file.read())

# # Belajar Python [Dasar] - 59 - tkinter | Standard Library Python GUI

# # GUI ---> Graphical User Interface
# import tkinter as tk
# from tkinter import ttk
# from tkinter.messagebox import showinfo

# # INIT
# window = tk.Tk()
# window.configure(bg="black")
# window.geometry("300x200")
# window.resizable(False,False)
# window.title("sapa dia")

# # VARIABLE dan FUNGSI
# NAMA_DEPAN = tk.StringVar()
# NAMA_BELAKANG = tk.StringVar()

# def tombol_click():
#     # fungsi ini akan dipanggil oleh tombol
#     pesan = f"halo {NAMA_DEPAN.get()} {NAMA_BELAKANG.get()}, wooooy"
#     showinfo(title="kocakbet",message=pesan)

# # frame input
# input_frame = ttk.Frame(window)
# # penempatan grid, pack, place
# input_frame.pack(padx=10,fill="x",expand=True)

# # komponen-komponen
# # 1.label nama depan 
# nama_depan_label = ttk.Label(input_frame,text="nama depan")
# nama_depan_label.pack(padx=10,fill="x",expand=True)
# # 2. entry nama depan
# nama_depan_entry = ttk.Entry(input_frame,textvariable=NAMA_DEPAN)
# nama_depan_entry.pack(padx=10,fill="x",expand=True)
# # 3.label nama belakang 
# nama_belakang_label = ttk.Label(input_frame,text="nama depan")
# nama_belakang_label.pack(padx=10,fill="x",expand=True)
# # 4. entry nama belakang
# nama_belakang_entry = ttk.Entry(input_frame,textvariable=NAMA_BELAKANG)
# nama_belakang_entry.pack(padx=10,fill="x",expand=True)
# # 5.tombol
# tombol_sapa = ttk.Button(input_frame,text="sapa",command=tombol_click)
# tombol_sapa.pack(fill='x',expand=True,padx=10,pady=10)

# # MAIN LOOP WINDOW
# window.mainloop()

# Belajar Python [Dasar] - 60 - Mengenal PIP
# PIP merupakan package python 
# cara melihat versi : pip list
# cara menginstal & uninstall : pip install numpy(==version,jika mau versi tertentu) & pip uninstall numpy

# Belajar Python [Dasar] - 61 - Package Numpy | contoh PIP
# import numpy as np
# ayam_1 = np.array([1, 2, 6, 7, 9, 4])
# print(ayam_1)
# # ini bisa dan lebih enak jika pengerjaan matrix

# # Belajar Python [Dasar] - 62 - Explore Pygame
# import pygame

# # ketika kita membuat game pasti ada 4 unsur ini : init,input data / database input,update asset,render display
# # init
# pygame.init()
# # variable running
# isrun = True
# # membuat display surface object
# window_panjang = 500
# window_lebar = 500
# window = pygame.display.set_mode((window_lebar, window_panjang))

# # object game
# # posisi
# x = 250
# y = 250

# # ukuran 
# panjang = 20
# lebar = 20

# # kecepatan gerak 
# speed = 10
# while isrun:
#     pygame.time.delay(10)
#     # user input,database input
#     for evemt in pygame.event.get():
#         if evemt.type == pygame.QUIT:
#             print("amsyong")
#             isrun = False
#     # ambil semua keyboard press
#     keys = pygame.key.get_pressed()

#     # ambil ke kiri
#     if keys[pygame.K_LEFT] and x > 0:
#         x -= speed

#     if keys[pygame.K_RIGHT] and x > window_lebar-lebar:
#         x += speed

#     if keys[pygame.K_DOWN] and y < window_panjang-panjang:
#         y += speed

#     if keys[pygame.K_UP] and y > 0:
#         y -= speed

#     # update asset
#     window.fill((255,255,255))
#     pygame.draw.rect(window,(255,0,0),(x,y,lebar,panjang))
#     # render ke display
#     pygame.display.update()

# pygame.quit()
# Belajar Python [Dasar] - 63 - __main__ sebagai gerbang program
# _main_ adalah top level code environment
# _name_ == "_main_"
# _name_ pada file program utama
# print(f"nilai _name_ pada f")

# Belajar Python [Dasar] - 64 - File I/O in Python (code with harry)
# Pengenalan Sintaks dasar : 
# f = open('namafile.txt' 'r/w') |r = read // w = write // a = appending | (kira-kira begitu)
# banyak mode yang lain sesuai kegunaannya masing masing 
# text = f.read | kita bebas bisa melanjutkan "f.write("teksnya apa") |
# print(text)
# f.close()
# ==> selalu ingat untuk tambahkan fungsi untuk menutup,tapi untuk lebih cepat kita bisa menambahkan seperti ini 
# with open("namafile.txt") as f: 
# ==> with digunakan untuk otomatis menutup

# Lanjut ke 50 code with harry 
f = open('myfile.txt', 'r')
while True:
    line = f.readline()
    if not line:
        break
    print(line)
    

